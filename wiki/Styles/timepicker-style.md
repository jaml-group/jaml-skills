# timepicker

`Styles.timepicker.*` -- time input/selector elements.

No extended style variants. Uses shared element styling.

```json jaml-playground
[
    {
        "type": "timepicker",
        "cap": "Pick a time"
    }
]
```
