# Explain

Read the owning reference and its linked detail page. Explain what the capability owns, how it works, when to select it, and material constraints. Distinguish authoring policy, implemented behavior and unresolved gaps.

Use the smallest useful example when it helps. Validate runnable examples statically; investigate diagnostics rather than assuming the analyzer is authoritative. Conceptual questions require no app scaffold, project-instruction changes or browser session.

Done: the explanation is supported by the owning contract and every runnable example has the verification actually available.
